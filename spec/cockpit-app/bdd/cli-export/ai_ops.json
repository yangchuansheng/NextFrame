{
  "module": "cli-export",
  "version": "v0.1",
  "description": "Placeholder export CRUD surface.",
  "crud": {
    "render-audio": {"cmd": "nextframe render <timeline.json> <out.mp4> --audio"},
    "render-target": {"cmd": "nextframe render <timeline.json> <out> --target <id>"},
    "render-crf": {"cmd": "nextframe render <timeline.json> <out.mp4> --crf <n>"},
    "probe": {"cmd": "nextframe probe <out.mp4> [--json]"}
  }
}
