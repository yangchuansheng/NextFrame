{
  "module": "safety-gates",
  "version": "v0.1",
  "description": "AI-facing operations for probing validation gates.",
  "crud": {
    "validate_timeline": {"cmd": "call validateTimeline(timeline)"},
    "resolve_time": {"cmd": "call resolveTimeline(timeline)"},
    "inspect_registry": {"cmd": "read src/scenes/index.js or REGISTRY for valid scene ids"}
  },
  "simulate": {
    "scenarios": [
      {
        "id": "sim-gates-1",
        "name": "broken reference feedback loop",
        "steps": ["Create a timeline with {after:'clip-missing'}", "Run validateTimeline", "Read hint for available anchors"],
        "assert": ["Validation fails structurally", "Hint is actionable for AI repair"]
      }
    ]
  }
}
