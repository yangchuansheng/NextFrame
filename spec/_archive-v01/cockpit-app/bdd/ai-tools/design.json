{
  "module": "ai-tools",
  "version": "v0.1",
  "description": "Every tool is a map entry with schema + handler.",
  "tool_contract": {
    "shape": "{ schema: { name, description, params[] }, handler(args) -> Result }",
    "non_goals": ["No CLI imports", "No mutation of input timeline"],
    "minimum_tool_count": 7
  }
}
