[package]
name = "project"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[lints.clippy]
unwrap_used = "deny"
expect_used = "warn"
panic = "deny"
