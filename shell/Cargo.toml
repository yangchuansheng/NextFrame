[package]
name = "shell"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bridge = { path = "../bridge" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tao = "0.35"
wry = "0.55"

[lints]
workspace = true
