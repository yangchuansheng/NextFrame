# Autopilot: 跑通 clips pipeline 端到端 + 桌面端高质量显示

- 启动: 2026-04-15 08:03
- Session: 57600966-055f-4591-821f-e9657b55544b
- 状态: executing
- 最小轮次: 5
- 当前轮次: 0/5
- 心跳 cron ID: 9a1a7b06

---

## 1. 用户原话

> 可以的，然后你开启自动化模式吧，就是确保这个流程OK 。 有问题你改好吧。 我出去了。 确保高质量。 确保桌面端OK。

## 2. 启动问答结果

| 问题 | 推断回答（用户已离开，未问） |
|---|---|
| **预期结果** | demo-opus-sinek/e01 智能切片 tab 真实显示 3 个 clip 卡片 + 中英双语 cue 字幕 + 词级卡拉OK 跟随播放 |
| **最小轮次** | 5（足够覆盖 dispatch + verify + fix gaps） |
| **边界条件** | 不动 protocol.rs / recorder / produce recipe / 旧项目数据 |
| **质量标准** | 端到端真实跑通；CLI 驱动；中文流畅完整句；卡拉OK跟嘴 |
| **失败策略** | 自己换方案继续。重要变更前发飞书 warning 知会 |

## 3. 目标

**用户回来打开 app → 进 demo-opus-sinek/e01/智能切片 tab →**
1. 看到 Simon Sinek TED 源（18:34）
2. 3 个 clip 卡片，缩略图首帧可见
3. 每个 clip 字幕显示**中英双语**（不只是英文）
4. 点 clip → 弹窗播放 + 词级卡拉OK + 中文按 cue 切换

**用户说的**: "确保流程 OK + 桌面端 OK"
**用户要的**: 端到端跑通 + 真实数据 + 桌面真能看到漂亮结果，不是 "命令能跑"

## 4. 原则

**固定 5 条**:
- loop 永不停转 — 每轮结束必须 ScheduleWakeup
- 自验证，不等人 — 截图 + Read + 写结论
- ally gpt 优先，subagent 仅用户明确时才用
- 改流程比改产出更有价值 — 提示词差就改提示词，不只改产出
- 根因优先 — 同一问题第 2 次出现做根因，第 3 次写 memory

**自定义**:
- 翻译质量必须高（完整句、口语感、不切残）→ 翻译完亲自读一遍
- 桌面端必须真正看到结果 → 用 `--eval-script` 自动 dump DOM + 截图

## 5. 约束

**固定**:
- [x] 心跳 cron 已注册 (ID: 9a1a7b06)
- [ ] 每轮必须有实质产出
- [ ] 验证不过不停转
- [ ] 飞书心跳每 5 分钟
- [ ] 目标未达成不能停

**自定义**:
- [ ] CLI 驱动 — agent 必须通过 `node nextframe.js source-translate --apply` 写产物，不绕过
- [ ] 不动旧项目 ted-demo / 爆款公式课
- [ ] 不动 protocol.rs / nf-recorder / produce recipe MD
- [ ] 工具修复必须能让任何 agent 复用，不是一次性 hack

## 6. 已知风险

| 风险 | 影响 | 应对 |
|---|---|---|
| Opus 调用 ally 又被 SIGTERM | 第 1 轮 Opus 已挂 | 直接自己干，不再派 ally Opus |
| smart-clips.js 加载新 translations 文件不兼容 | 桌面端无中文 | 跑 eval-script 自检，发现 → 改 smart-clips.js |
| sentences.json 部分 clip 字幕格式问题 | 翻译数据不齐 | 校验后再 apply |
| 词级卡拉OK 在 eval-script 自动模式下不触发（autoplay 限制） | 验证脚本误报 | 加 `muted autoplay` 强制起播或主动 video.play() |
| 网络问题 `cargo run` 拉依赖慢 | 启动慢 | 提前 cargo build |

## 7. 任务清单（全局）

### 阶段 A: 完成 pipeline 数据产出
- [ ] plan.json — Claude 自己挑 3 段 highlight
- [ ] cut — CLI 切 3 个 mp4
- [ ] translate clip 1/2/3 — Claude 自己翻，CLI --apply 落盘
- [ ] polish clip 1/2/3 — Claude 自己写文案，CLI --apply 落盘

### 阶段 B: 桌面端验证
- [ ] cargo run -p nf-shell-mac 启动
- [ ] eval-script 自动导航到 demo-opus-sinek/e01/智能切片
- [ ] dump 验证：3 clip 卡 + 中文字幕显示 + 弹窗 karaoke
- [ ] 截图 /tmp/autopilot-final.png + Read 确认

### 阶段 C: 缺啥补啥
- [ ] smart-clips.js 加载新 translations 不兼容 → 改
- [ ] autoplay/karaoke 在自动化下不触发 → 改 muted autoplay
- [ ] 任何一项不达标 → ally gpt 修

### 阶段 D: 收尾
- [ ] 单个原子 commit
- [ ] 飞书 done 通知
- [ ] CronDelete 心跳

## 8. 每轮方向

- **Round 1**: 自己跑 plan + cut + translate clip 1 + 启动 app 看是否能加载真实数据
- **Round 2**: 翻 clip 2/3 + polish 全部 + 桌面端 eval-script 验证
- **Round 3**: 验证报告分析 + 缺口修复（smart-clips.js / autoplay）
- **Round 4**: 重新验证 + 截图确认
- **Round 5**: 收尾 commit + 飞书通知

## 9. 飞书通知计划

- 启动: info "autopilot 启动：clips pipeline 端到端 + 桌面端验证，5 轮"
- Round 完成: 跟着心跳走（每 5 min auto info）
- 桌面端验证完成: info "桌面端验证通过"
- 全部完成: done "autopilot 完成：M 轮"
- 卡住: warning

---

## Round 1 — 08:03

**做了什么**: 注册心跳 cron + 写计划 + 建任务清单
**怎么做的**: 自己做（autopilot 启动配置）
**验证**: cron ID 9a1a7b06 已确认
**结果**: done
**学到什么**: ally 任务可能被 SIGTERM 抢占，autopilot 模式应直接自己干
**流程改进**: 不再派 ally opus，自己执行
**下一步**: 启动飞书启动通知，跑 plan + cut + 翻第一个 clip
