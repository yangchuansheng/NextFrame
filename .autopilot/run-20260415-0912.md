# Autopilot: 桌面端智能剪辑 tab — DOM 引擎预览 + 多轨道同步

- 启动: 2026-04-15 09:12
- Session: ce22c029
- 状态: executing
- 最小轮次: 5
- 当前轮次: 2/5
- 心跳 cron ID: a837f815

---

## 1. 用户原话

> 对你现在唯一的目的就是可以给我做到能够再桌面端的智能剪辑 tab 看剪辑播放预览和多轨道。开启自动化迭代迭代几轮。
> 展示引擎用我们的新的 DOM 引擎。注意模块设计等等。
> 不跟项目关联，确保能够跑通。

## 2. 启动问答结果

| 问题 | 用户回答 |
|---|---|
| **预期结果** | 桌面端打开智能剪辑 tab → preview 用 DOM 引擎渲染 scene → 点播放能动 → 底部 timeline 多轨道同步跟 playhead 走 |
| **最小轮次** | 5 |
| **边界条件** | 不改 preview-engine.js / transport.js / scene-bundle.js（已打磨好的） |
| **质量标准** | 能播放 + 轨道同步 = 满意 |
| **失败策略** | 自己换方案继续 |

## 3. 目标

用户回来打开桌面端 app → 点智能剪辑 tab → 看到 preview 区有画面 → 点播放能动 → 底部 timeline 轨道跟着走。

- 用户说的: 用 DOM 引擎跑通预览和多轨道
- 用户要的: 一个能跑的编辑器预览，证明 DOM 引擎链路通了

## 4. 原则

- loop 永不停转 — 每轮结束必须 ScheduleWakeup
- 自验证，不等人 — 截图 + Read + 写结论
- ally gpt 优先 — 主 agent 只做决策和验证
- 改流程比改产出更有价值
- 根因优先 — 同一问题第 2 次出现做根因分析

**自定义原则：**
- DOM 引擎已就绪 — preview-engine.js + transport.js + scene-bundle.js 不动，只改 editor.js 接入层
- 去耦合 — 不依赖 bridgeCall/项目数据，用固定 demo-timeline.json

## 5. 约束

- [x] 心跳 cron 已注册 — cron ID: a837f815
- [ ] 每轮必须有实质产出
- [ ] 验证不过不停转
- [ ] 飞书心跳每 5 分钟
- [ ] 目标未达成不能停
- [ ] lint 全通过 — bash scripts/lint-all.sh
- [ ] 不改 preview-engine.js / transport.js / scene-bundle.js

## 6. 已知风险

| 风险 | 影响 | 应对 |
|---|---|---|
| editor.js 有重复播放代码和 transport.js 冲突 | 播放/seek 行为不一致 | ralph 任务已要求清理 |
| scene-bundle.js 里的 scene ID 可能和 demo JSON 不匹配 | 渲染空白 | 检查 scene-bundle 注册的 ID |
| WKWebView 里 fetch() 加载本地 JSON 可能有限制 | 数据加载失败 | 可改用 inline 或 bridgeCall fs.read 兜底 |
| editor.js 里 buildEditorTimelinePath 写死了 timeline.json | save/preview 路径错误 | ralph 任务已要求修改 |

## 7. 任务清单

### 阶段 A: ally ralph 完成基础改造
- [ ] ralph 跑完（executor + reviewer 10/10）
- [ ] 合并 worktree 到 main

### 阶段 B: 桌面端验证
- [ ] 编译启动 app
- [ ] 打开智能剪辑 tab 截图
- [ ] 确认 preview 区渲染了 scene 内容
- [ ] 确认播放能动
- [ ] 确认 timeline 轨道同步

### 阶段 C: 修复问题（如有）
- [ ] 根据验证结果修复
- [ ] 重新验证

### 最终验证
- [ ] 全链路端到端跑通
- [ ] 所有 TaskList 子任务 completed
- [ ] 飞书通知发送

## 8. 每轮方向

| 轮次 | 大致方向 | 执行者 |
|---|---|---|
| 1 | 等 ralph 完成，检查结果 | 自己 |
| 2 | 合并 worktree，编译启动，截图验证 | 自己 |
| 3 | 根据验证结果修复问题 | 自己 / ally |
| 4 | 二次验证 + 打磨 | 自己 |
| 5 | 最终验证 + 飞书通知 | 自己 |

## 9. 飞书通知计划

| 时机 | status | 消息模板 |
|---|---|---|
| 启动 | info | "autopilot 启动：智能剪辑 DOM 引擎预览，计划 5 轮" |
| 心跳 | info | "[M/5轮] 智能剪辑 DOM 预览... → 当前：{进展}" |
| 完成 | done | "autopilot 完成：{M} 轮，编辑器预览可播放 + 轨道同步" |
| 卡住 | warning | "第 {M} 轮卡住：{原因}" |

- 上次通知时间: 未发

---

## 进化记录

### Round 1 — 09:21

**做了什么**: ally ralph 完成 editor DOM 改造，合并到 main
**怎么做的**: ally ralph gg preset，GPT executor + reviewer，1 轮 10/10 通过
**验证**: lint-all.sh 全通过，editor.js 306 行，demo-timeline.json 5 layers
**结果**: done — 合并成功，worktree 已清理
**学到什么**: ralph 一轮就搞定了，task.md 写得够清晰时效率很高
**流程改进**: 无
**下一步**: 启动 app，截图验证智能剪辑 tab

### Round 2 — 09:28

**做了什么**: ADR-010 写入 + __nfEditorDiagnose 自验证函数 + 集成到 __nfDiagnose
**怎么做的**: 自己做。用户反馈"别用 AppleScript，自验证能力写在代码里"
**验证**: lint 全通过，commit 成功 16adf0c
**结果**: done — 代码层面的自验证能力已具备
**学到什么**: 外部操作（AppleScript）不可靠，验证能力必须内建
**流程改进**: 以后先加 diagnose 函数再验证，不依赖外部工具截图
**下一步**: 用户操作 app 进入智能剪辑 tab，用 diagnose 确认状态
