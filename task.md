# Task — Fix GPT Audit Findings to reach 9.5+

## Goal
Fix the code quality issues found by the GPT audit (score 8.5/10) to push it to 9.5+.

## Requirements

### 1. scene-bundle.js (735 lines) — exceeds 500-line limit
- This file is at `src/nf-runtime/web/js/scene-bundle.js`
- It's auto-generated ("Auto-generated by nextframe bundle-scenes. Do not edit.")
- Fix: exclude it from the file-size check in `scripts/lint-all.sh` by adding `-not -name 'scene-bundle.js'` to the find command in gate 4

### 2. unsafe impl/fn missing SAFETY comments (9 occurrences)
Add `// SAFETY:` comment above each of these:
- `src/nf-shell-mac/src/ipc.rs:62` — `unsafe impl NSObjectProtocol for BridgeHandler`
- `src/nf-shell-mac/src/ipc.rs:64` — `unsafe impl WKScriptMessageHandler for BridgeHandler`
- `src/nf-shell-mac/src/protocol.rs:36` — `unsafe impl NSObjectProtocol for $name`
- `src/nf-shell-mac/src/protocol.rs:39` — `unsafe impl WKURLSchemeHandler for $name`
- `src/nf-shell-mac/src/protocol.rs:41` — `unsafe fn web_view_start_urlscheme_task`
- `src/nf-shell-mac/src/protocol.rs:50` — `unsafe fn web_view_stop_urlscheme_task`
- `src/nf-shell-mac/src/window.rs:84` — `unsafe impl NSObjectProtocol for WindowDragHandler`
- `src/nf-shell-mac/src/window.rs:86` — `unsafe impl WKScriptMessageHandler for WindowDragHandler`
- `src/nf-shell-mac/src/window.rs:218` — `unsafe fn inset_traffic_lights`

For `unsafe impl`: "SAFETY: objc2 trait impl — type inherits from NSObject, callbacks run on main thread"
For `unsafe fn`: "SAFETY: caller ensures valid NSWindow pointer on main thread"

### 3. nf-tts/nf-shell-mac errors without Fix guidance
Check error messages in:
- `src/nf-tts/src/backend/volcengine/mod.rs`
- `src/nf-tts/src/backend/volcengine/client.rs`
- `src/nf-shell-mac/src/webview.rs`

For each `Err(...)` or `bail!(...)` that doesn't include "Fix:" or "Internal:", add a Fix suggestion.

## Technical Constraints
- `cargo check --workspace` must pass
- `cargo clippy --workspace -- -D warnings` must pass
- `bash scripts/lint-all.sh` must pass all 10 gates
- No behavior changes — only add comments, fix lint config, add Fix messages

## Verification Commands
cargo check --workspace
cargo clippy --workspace -- -D warnings
bash scripts/lint-all.sh
grep -rn 'unsafe' src/ --include='*.rs' | grep -v target | grep -v test | grep -v SAFETY | grep -v allow | grep -v unsafe_code | grep -v '#\[unsafe' | wc -l
